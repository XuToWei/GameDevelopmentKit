namespace UnityGameFramework.Extension
{
    public enum NetworkServiceState
    {
        Undefined = 0,
        UnInitialized,
        Initialized,
        Connecting,
        Connected,
        ConnectionFailed,
        Disconnecting,
        Disconnected,
    }
}
